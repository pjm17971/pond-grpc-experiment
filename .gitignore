node_modules/
dist/
build/
*.tsbuildinfo
coverage/
.vite/
.turbo/
.cache/

.DS_Store
*.log
.env
.env.local
.env.*.local

# Claude Code per-developer state — keep launch.json so any agent can preview-start the dev server.
.claude/*
!.claude/launch.json

# Generated proto types — regenerated via `pnpm codegen` (requires protoc).
packages/shared/src/grpc/
